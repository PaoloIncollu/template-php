/*const { createApp } = Vue;

createApp({
    data() {
        return {
            message:'Template PHP'
        };
    },
    created() {
        this.search();
    },
    methods: {
        search() {
            axios
                .get('URL', {
                    params: {
                        
                    }
                })
                .then((res) => {
                    console.log(res.data);

                    this= res.data;
                });
        }
    }
}).mount('#app');*/